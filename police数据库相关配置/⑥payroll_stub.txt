ALTER TABLE PAYROLL_STUB
ADD (DESCRIPTION VARCHAR2(255 BYTE),
ISSUE_ID VARCHAR2(20 BYTE)，
ISSUE_NAME VARCHAR2(20 BYTE));

UPDATE PAYROLL_STUB
SET ISSUE_ID = CASE
  WHEN PAYROLL_NUMBER <= 20 THEN '1109272'
  ELSE '4619083'
  END,
  ISSUE_NAME = CASE
  WHEN PAYROLL_NUMBER <= 20 THEN '黄燕'
  ELSE '黄周军'
  END,
  DESCRIPTION = '正常发放';

ALTER TABLE PAYROLL_STUB
ADD CONSTRAINT FK_ISSUE_POLICE
FOREIGN KEY (ISSUE_ID) REFERENCES POLICEMEN(POLICE_NUMBER);
